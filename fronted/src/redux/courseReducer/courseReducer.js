const iniState = {
    courses: [],
    isLoading: false,
    isError: false
}

export const courseReducer = (state = iniState, {
    type,
    payload
}) => {
    switch (type) {
        default:
            return state
    }
}